import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      alignItems: 'center',
      marginBottom: 12,
      marginLeft: 8,
    },
    text: {
      color: '#FFFFFF',
      fontSize: 16,
    },
    checkboxContainer: {
      marginTop: 16,
      paddingHorizontal: 16,
      paddingVertical: 8,
      
    },
  });